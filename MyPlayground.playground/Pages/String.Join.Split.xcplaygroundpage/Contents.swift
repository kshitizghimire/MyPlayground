// only works with Element with String type
let a = ["2", "3", "4"].joined(separator: ",")
print(a)

// return of split is SubString type
let subString = "st ri n g".split(separator: " ")
print(subString)
